# Static Web App - TodoList
Simple static web app that uses:
- Azure Functions (Http triggers) as backend.
- Blazor WASM as frontend.

His main goal is to test the main features of the Azure Static Web App.
