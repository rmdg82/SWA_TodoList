{
  "navigationFallback": {
    "rewrite": "/index.html"
  },
  "routes": [
    {
      "route": "/api/todos/*",
      "methods": [ "GET", "POST", "PUT", "DELETE" ],
      "allowedRoles": [ "authenticated" ]
    }
  ]
}