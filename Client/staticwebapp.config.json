{
  "navigationFallback": {
    "rewrite": "/index.html"
  },
  "routes": [
    {
      "route": "/api/todos",
      "methods": [ "GET", "POST" ],
      "allowedRoles": [ "authenticated" ]
    }
  ]
}